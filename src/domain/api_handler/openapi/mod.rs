
	// let spec = parse_openapi(opt.input).deref_all();

	// // println!("{:?}", spec.servers);

	// let _server = &spec.servers[0].url;
	// // println!("{}", server);

	// let path = &spec.paths["/leaderboard"];
	// println!("{:?}", path);